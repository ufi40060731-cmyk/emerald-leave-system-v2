# v14.4.2 Language hotfix

- Keeps Traditional Chinese, English, and Thai as static HTML options.
- Adds visible language quick-selection buttons on the login page.
- No longer clears the language list while rebuilding selectors.
- Embeds the translation dictionary in `index.html`; the login page works even if `i18n.js` is unavailable.
- Adds explicit select/option text colors for Windows Edge.
- Keeps GitHub Pages deployment and Thailand holiday automation unchanged.
